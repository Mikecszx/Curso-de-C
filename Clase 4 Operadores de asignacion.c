#include <stdio.h>

int main (){
	int a,b,c;
	a=b=c=10;
	
	
	
	printf("Los valores son: %i, %i, %i",a, b, c );
	
	int d =10;
	
	d = d + 10; //los valores a lo largo del codigo pueden variar y pueden asignarse valores nuevos cuando se necesiten
				//es lo mismo que si se usara d+=10
				//se puede hacer con resta, multiplicacion, division
	
	printf("El valor es: %i",d);
	
	
	
	
	
	return 0;
}
